
import { Toolchain } from './lib';

/**
 * Represents a wrapper around the `Toolchain`
 */

// Insert your options here and then you will be able
// to customize your app configuration

const instance = new Toolchain();
instance.build();